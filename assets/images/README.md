Drop real photos/screenshots here (headshot, project photos, etc.) and
reference them from the HTML/data files, e.g. `assets/images/headshot.jpg`.

This file just keeps the folder tracked in git until you add real images —
feel free to delete it once the folder has real content.
